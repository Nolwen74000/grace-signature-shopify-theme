<style>
  .error-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px;
    background: var(--color-background, #0a0a0a);
    color: var(--color-text, #f5f5f0);
  }

  .error-container {
    text-align: center;
    max-width: 600px;
  }

  .error-code {
    font-family: var(--font-heading, 'Cormorant Garamond', serif);
    font-size: clamp(6rem, 15vw, 10rem);
    font-weight: 200;
    letter-spacing: 0.1em;
    line-height: 1;
    color: var(--color-gold, #d4af37);
    margin-bottom: 20px;
  }

  .error-title {
    font-family: var(--font-heading, 'Cormorant Garamond', serif);
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    font-weight: 300;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 30px;
  }

  .error-message {
    font-family: var(--font-body, 'Montserrat', sans-serif);
    font-size: 1.05rem;
    line-height: 1.8;
    color: rgba(245, 245, 240, 0.7);
    margin-bottom: 50px;
  }

  .error-actions {
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
  }

  .error-btn {
    display: inline-block;
    padding: 18px 50px;
    font-family: var(--font-body, 'Montserrat', sans-serif);
    font-size: 0.85rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    text-decoration: none;
    transition: all 0.4s ease;
  }

  .error-btn-primary {
    background: var(--color-gold, #d4af37);
    border: 1px solid var(--color-gold, #d4af37);
    color: #0a0a0a;
  }

  .error-btn-primary:hover {
    background: transparent;
    color: var(--color-gold, #d4af37);
  }

  .error-btn-secondary {
    background: transparent;
    border: 1px solid rgba(212, 175, 55, 0.4);
    color: var(--color-text, #f5f5f0);
  }

  .error-btn-secondary:hover {
    border-color: var(--color-gold, #d4af37);
    color: var(--color-gold, #d4af37);
  }

  @media (max-width: 768px) {
    .error-page {
      padding: 100px 25px;
    }
  }
</style>

<div class="error-page">
  <div class="error-container">
    <p class="error-code">404</p>
    <h1 class="error-title">Page introuvable</h1>
    <p class="error-message">
      La page que vous recherchez n'existe plus ou a été déplacée.<br>
      Nous vous invitons à poursuivre votre exploration.
    </p>
    <div class="error-actions">
      <a href="{{ routes.root_url }}" class="error-btn error-btn-primary">Retour à l'accueil</a>
      <a href="{{ routes.collections_url }}" class="error-btn error-btn-secondary">Découvrir nos collections</a>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Page 404",
  "settings": [],
  "presets": [
    {
      "name": "Page 404"
    }
  ]
}
{% endschema %}
